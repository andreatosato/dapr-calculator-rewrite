apiVersion: apps/v1
kind: Deployment
metadata:
  name: sub-app
  labels:
    app: sub-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sub-app
  template:
    metadata:
      labels:
        app: sub-app
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "sub-app"
        dapr.io/app-port: "80"
    spec:
      containers:
      - name: sub-app
        image: dapriotregistry.azurecr.io/sub:latest
        ports:
        - containerPort: 80
        imagePullPolicy: Always